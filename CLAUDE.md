# CLAUDE.md

## Error Message Guidelines

Error messages must help users understand what went wrong and how to fix it.

- State what was expected and what was actually found
- Include a fix or workaround when one exists
- Include concrete values detected (column names, table names, counts, etc.)
